---
screen_name: 丽都周纪
appears_in: [快捷手册-日常]
last_updated: 2026-07-12
source_image: screens/丽都周纪/BINGO主画面.webp
---

# 丽都周纪(周常积分奖励)

快捷手册-日常 → 丽都周纪,周常 BINGO 积分玩法(`ridu_weekly` app)。**纯领奖励(无战斗)**:领积分行 → 领取奖励。

## 入口 + 状态流转

大世界 → 快捷手册(`大世界-普通`.`快捷手册`) → **快捷手册-日常**(TAB-日常) → 点「丽都周纪」入口 → **丽都周纪-BINGO 主画面** → 领积分 → 领取奖励 → 返回大世界。

流转(`RiduWeeklyApp` `@operation_node` 链):
返回大世界 → 日常(`round_by_goto_screen('快捷手册-日常')`) → 丽都周纪(click `丽都周纪` area) → 领取积分(积分行-1/2/3 OCR "100" 白色) → 领取奖励 → 完成后返回。

## BINGO 主画面(已建档)

丽都周纪画面。screen_info `丽都周纪`(`ridu_weekly.yml`):
- **左上**:`丽都周纪`(`414,840-596,900`,标题/入口)/ `街区` / 返回(back 模板,通用)。
- **BINGO 网格**(左侧 ~`120~900,330~880`):3×3 网格,完成连线得「三格连线额外奖励」(150)+ `积分特赏`(右上)。
- **任务列表**(右侧 `代办列表` `534,400-1346,776`):任务目标(X/X)+ 描述 + 积分(1100/1200/1300/1700/2100)+ 状态(`领取` / `未完成` / `确认代办`)。
- **任务状态 area**(app 不直接使用,任务行状态锚点):`确认代办`(`818,798-1096,848`,text「确认」)/ `领取`(`452,804-626,848`)/ `未完成`(同位)/ `任务区域`(`168,260-1052,954`)。
- **积分行**:`积分行-1/2/3`(`120,410~1055,925`,3 行)—— app `claim_score` 在此 OCR "100"(白色 `250~255` `lcs=1.0`)点击领积分。
- **领取奖励**:`领取奖励`(`1558,314` 点坐标)—— app `confirm_schedule` 点击。

source_image: screens/丽都周纪/BINGO主画面.webp

## 识别快照

### 1. BINGO 主画面(source_image: screens/丽都周纪/BINGO主画面.webp)

- 匹配画面:**无精准命中**(本 screen area 全 `id_mark=false`,analyze 不匹配)。
- 匹配 area(误匹配,均非本 screen):

  | area | 误匹配自 | 类型 | 文本 |
  |---|---|---|---|
  | 街区 | 委托助手/随便观-游历 等 | text | 街区 |
  | 返回 | 画面-通用/迷失之地-特遣调查 | template | — |

- 判定:靠 BINGO 网格 + 「任务目标 X/X」+「积分特赏」文字 + 入口路径(从快捷手册-日常点来)。
- 全量 OCR:街区 / 三格连线额外奖励 150 / 积分特赏 / 任务目标 2/2 / 在报刊亭刮刮卡或算卦犬占卜 / BINGO(网格多) / 积分 1100·1200·1300·1700·2100 / 可获得 100 / 已完成 / 累计获取积分 1700 / 潜能影集。
- 流转:app `claim_score` 积分行 OCR "100" 领 → 无 100 后(`round_retry`)→ `confirm_schedule` 点 `领取奖励`。

## 备注

- **纯领奖励 app**(无战斗)。入口 快捷手册-日常(已建,[快捷手册-日常.md](快捷手册-日常.md))→ 丽都周纪;同类 email/engagement_reward/city_fund。
- ⚠️ area 全 `id_mark=false` → analyze **无精准匹配**,靠 BINGO 网格 + 任务目标 文字 + 入口路径判定;易与共享 `街区`+back 模板的画面误判。
- `领取奖励` area pc_rect 是点坐标(`1558,314` 同点,screen_info yml 同为点坐标)—— backend 校验 `x2>x1, y2>y1` 会拒,合法矩形待补(需实拍量);doc 如实记录现状。
- 信息源:screen_info(`丽都周纪` area)+ `RiduWeeklyApp` 流程 + BINGO 主画面截图。
