---
screen_name: 仓库-驱动仓库-驱动盘拆解
appears_in: [仓库-驱动仓库]
last_updated: 2026-07-11
source_image: screens/仓库-驱动仓库-驱动盘拆解/默认.webp
status: 默认态 + 快速选择弹窗已建档;拆解确认弹窗待补(需选中驱动盘 + 真拆解)
---

# 仓库-驱动仓库-驱动盘拆解

## 何时出现 + 状态流转

仓库-驱动仓库点「按钮-拆解」→ **驱动盘拆解画面**。`drive_disc_dismantle` app 批量拆解低级驱动盘(S/A/B 及以下 / 已弃置)。

> **跨画面 op 联动**:app `back_at_first`(is_start_node)+ `back_at_last` 均委托 `BackToNormalWorld` —— 入口先回大世界再进仓库拆解,出口拆解完回大世界。

| 子态 | 入口 | 出口 |
|---|---|---|
| ① 默认 | 驱动仓库点拆解 | 快速选择 → ②;拆解(选中后)→ ③;街区/返回 → 驱动仓库 |
| ② 快速选择弹窗 | 默认点「快速选择」 | 选条件 + 确认 → ①(已选中);关闭 → ① |
| ③ 拆解确认弹窗 ⚠️ 待补 | ① 选中(>0)后点拆解 | 确认 → 拆解执行;取消 → ① |

## 识别特征(稳定锚点)

- **标题「驱动盘拆解[0/300]」**(@~(189,38),`标题-驱动盘拆解` area text`驱动`,`id_mark`)。
- **按钮-快速选择**(@~(213,1009),`id_mark`)。
- **按钮-拆解**(@~(1688,884))。
- 显示已装备开关(OFF,@~(1706,36))。
- **易变值**:拆解数量 `0/300`、驱动盘等级(**动态**)。

## 可交互元素

- **快速选择**(弹筛选弹窗 ②)。
- **智能弃置**(@~(509,1013),展示性 —— app 用 `按钮-全选已弃置` 表达批量弃置,非此按钮)。
- **拆解**(需选中,弹确认 ③)。
- 显示已装备开关。
- 驱动盘格子(选中)。

## 识别快照

### 1. 默认态(`drive_disc_dismantle_default`,2026-07-11 核对)

- 匹配画面:**曾误匹配「快捷手册-日常」(`is_precise=True`,`TAB-日常` conf 0.788)** —— commit `07e281c3`(2026-07-08「修复驱动盘拆解页面识别失败」)已修;当前默认态匹配状态待实拍核实(标题/快速选择 area 在 `drive_disc_dismantle.yml`)。
- 匹配 area(本 screen `id_mark`):

  | area | 类型 | 文本 | 位置 |
  |---|---|---|---|
  | 标题-驱动盘拆解 | text | 驱动 | 168,17-432,92 |
  | 按钮-快速选择 | text | 快速选择 | 134,968-407,1068 |
  | 按钮-拆解 | text | 拆解 | 1626,876-1808,926 |

- OCR:驱动盘拆解[0/300] / 显示已装备 OFF / 快速选择 / 智能弃置 / 拆解 / 等级0 网格。
- 布局(vision):标题顶部 / 驱动盘网格(左,等级0)/ 右侧详情 / 底部 快速选择+智能弃置+拆解。

### 2. 快速选择弹窗(`drive_disc_dismantle_filter`,2026-07-11 核对)

- 匹配画面:**仓库-驱动仓库-驱动盘拆解**(`is_precise=False`,area 全中)。
- 匹配 area:

  | area | 文本 | conf | 位置 |
  |---|---|---|---|
  | 按钮-全选已弃置 | 全选已弃置 | 0.999 | 744,472 |
  | 按钮-A及以下 | A及以下 | 0.999 | 1093,578 |
  | 按钮-S及以下 | S及以下 | 0.999 | 755,635 |
  | 按钮-B | B | 0.995 | 781,580 |
  | 按钮-快速选择-确认 | 确认 | 0.999 | 1089,764 |

- OCR:快速选择 / 在已弃置·未弃置驱动盘中选择 / 全选已弃置 / B / A及以下 / S及以下 / 符合条件数量0 / 重置 / 确认。
- 布局(vision):**模态弹窗**(遮罩透拆解画面),选择条件(全选已弃置 / B / A及以下 / S及以下)+ 符合数量 + 重置 / 确认 / 关闭。

### 3. 拆解确认弹窗 ⚠️ 待补

需选中驱动盘(>0)+ 点拆解触发。本次账号驱动盘全等级 0,快速选择匹配 0 个,无法触发拆解确认。待有可拆解驱动盘时补 `确认弹窗.webp`。

## 备注

- **已建模** `drive_disc_dismantle.yml`(9 area:标题 / 快速选择 / 全选已弃置 / A及以下 / S及以下 / B / 快速选择-确认 / 拆解 / 拆解-确认)。
- **识别问题**:默认态曾误匹配「快捷手册-日常」(commit `07e281c3` 已修,当前待实拍核实);快速选择弹窗态 area 全中但 `is_precise=False`。
- **拆解确认弹窗待补**(③ 子态)。
- `drive_disc_dismantle` app 流程:快速选择 → 选等级 → 选弃置 → 快速选择确认 → 拆解 → 拆解确认 → 返回。
