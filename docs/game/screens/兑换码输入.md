---
screen_name: 兑换码输入
appears_in: [菜单-更多功能]
last_updated: 2026-07-11
source_image: screens/兑换码输入/默认.webp
status: 独立画面;screen_info 缺口(无独立 screen,area 借 menu.yml);兑换结果弹窗未覆盖
---

# 兑换码输入

## 何时出现 + 状态流转

菜单-更多功能点「兑换码」→ **兑换码输入**(模态弹窗)。`redemption_code` app 输入兑换码兑换奖励。

| 出口 | 动作→下一态 |
|---|---|
| 输入 + 兑换 | 点输入框 → `keyboard.type` 码 → 点兑换 → 兑换结果弹窗(成功/失败,**未覆盖,需有效兑换码**;app `confirm_code` 当前不区分成功/失败 UI,统一再次点 + 记 `used_code`) |
| 关闭X | → 菜单-更多功能 |
| 全部兑换完毕 | app `back` 节点 → `BackToNormalWorld` → 大世界(`redemption_code_app.py:101-105`) |

## 识别特征(稳定锚点)

- 标题「兑换码」(@~(655,357))。
- 输入框(占位「请输入兑换码」@~(880,555),含粘贴按钮)。
- 兑换按钮(@~(950,697),亮色可点)。
- 关闭X(@~(1222,336))。

## 可交互元素

- **输入框**(含粘贴按钮;`redemption_code` app 用 `keyboard.type` 输入,非 MCP `input_text`)。
- **兑换按钮**(亮色可点,空输入也可点)。
- **关闭X**(右上)。
- 模态:半透明遮罩透出菜单-更多功能(vision 确认)。

## 识别快照

### 1. 输入态(2026-07-11 核对)

- 匹配画面:**无精准匹配**(菜单 / 画面-通用 / 菜单-更多功能 均 `is_precise=False` 模糊)。
- 匹配 area(模糊命中,跨 screen):

  | area | 所属 screen | 类型 | conf | 位置 |
  |---|---|---|---|---|
  | 兑换码兑换 | 菜单(`menu.yml`) | text「兑换」 | 0.963 | 950,697 |
  | 关闭 | 画面-通用 | template | 0.966 | 1222,336 |

- OCR:兑换码 / 请输入兑换码 / 兑换。

### 2. 兑换结果弹窗(⚠️ 待补:需有效兑换码;成功「兑换成功」/ 失败「兑换码无效」)

## 备注

- **⚠️ screen_info 缺口**:本画面**无独立 screen 收录**。`兑换码输入框` / `兑换码兑换` area 定义在 `menu.yml`(菜单);`redemption_code` app 导航分两段:**打开菜单「更多」+ 点「兑换码」入口**走 OCR(`round_by_ocr_and_click`,文本「更多」/「兑换码」+ `get_area('菜单',...)` 限定区域),**点输入框**走 `round_by_click_area('菜单','兑换码输入框')` 直点 area(非 OCR)。可考虑新建独立 screen(如 `兑换码输入`)提升识别精度 + 收录输入框/标题锚点。
- 模态弹窗(半透明遮罩透菜单-更多功能)。
- **⚠️ 兑换结果弹窗未覆盖**:需有效兑换码触发(成功「兑换成功」/ 失败「兑换码无效」弹窗),本次未截,待补。
- 输入实现:`redemption_code_app.py:83` `keyboard.type(code)` + sleep 6s(等界面响应),再点兑换。
