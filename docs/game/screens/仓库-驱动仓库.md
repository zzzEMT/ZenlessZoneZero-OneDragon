---
screen_name: 仓库-驱动仓库
appears_in: [仓库-音擎仓库]
last_updated: 2026-07-11
source_image: screens/仓库-驱动仓库/默认.webp
---

# 仓库-驱动仓库

## 何时出现 + 状态流转

仓库-音擎仓库点右上「TAB-驱动盘」(@~(1549,168))→ **仓库-驱动仓库**。驱动盘管理(查看 / 弃置 / 拆解)。`drive_disc_dismantle` app 经此进拆解画面。

| 出口 | 动作→下一态 |
|---|---|
| 按钮-拆解 | 点拆解 → 仓库-驱动仓库-驱动盘拆解 |
| 左上角-街区 | → 大世界-普通 |
| TAB-音擎仓库 | → 仓库-音擎仓库 |

## 识别特征(稳定锚点)

- **标题「驱动仓库[1879/3000]」**(@~(74,124),`标题-驱动仓库` area text`驱动仓库`,`id_mark`)。
- **按钮-拆解**(@~(334,912),`id_mark`,goto 驱动盘拆解)。
- **左上角-街区**(@~(286,34),goto 大世界-普通)。
- 驱动盘网格(等级 15 等)。
- **易变值**:驱动盘数量 `1879/3000`、格子等级/属性(**动态**)。

## 可交互元素

- 驱动盘格子(点击查看详情 / 选中)。
- **按钮-拆解**(进拆解画面)。
- 弃置 / 锁定(底部 @~(1686,1028)/(1821,1028))。
- TAB(音擎仓库 / 驱动盘 切换)。
- 左上角-街区(回大世界)。

## 识别快照(`storage_drive_disc_default`,2026-07-11 核对)

- 匹配画面:**模糊**(委托助手 / 画面-通用 / 零号空洞-事件 / 迷失之地 等 `is_precise=False`),**未精准命中「仓库-驱动仓库」** —— `标题-驱动仓库` area(text`驱动仓库`)按理应命中,实际未精准(识别待加强)。
- OCR:驱动仓库[1879/3000] / 街区 / 拆解 / 弃置 / 锁定 / 等级15。

## 备注

- **已建模** `storage_drive_disc.yml`(3 area:标题 / 拆解 / 街区)。识别模糊(未精准),与驱动盘拆解画面同属近期 commit「修复驱动盘拆解页面识别失败」的修复范围。
- 入口:仓库-音擎仓库右上「TAB-驱动盘」(`storage_wengine.yml` 的 `TAB-驱动盘` area)。
