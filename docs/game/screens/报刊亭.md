---
screen_name: 报刊亭
appears_in: [大世界]
last_updated: 2026-07-12
source_image: screens/报刊亭/默认.webp
status: 报刊亭场景 + 刮态已建;嗷呜对话/确认弹窗待补
---

# 报刊亭

## 何时出现 + 状态流转

`scratch_card`(刮刮卡)app 场景。大世界传送到「六分街-报刊亭」附近 → `move_w`+`interact`(F)→ **报刊亭场景**(独立画面,非大世界子态)→ 点「刮刮卡」→ 刮态 → drag 刮层 → 确认。

| 子态 | 入口 | 出口 |
|---|---|---|
| ① 报刊亭场景 | 大世界 F 交互(嗷呜 / 报刊亭) | 点刮刮卡 → ②;返回 → 大世界 |
| ② 刮态 | 点刮刮卡 | drag 刮层 1-3 → ③;返回 → ① |
| ③ 确认弹窗 ⚠️ 待补 | drag 刮完 | 按钮-同类型确认 → 领取 → 返回 |

> 嗷呜对话(叫醒)子态:本次未捕捉(点刮刮卡直接进刮态;嗷呜对话分支条件待确认)。

## 识别特征(稳定锚点)

- 报刊亭场景:城市快报 / CITY NEWS 墙 + 刮刮卡入口(`刮刮卡` area @94,956-172,1050)。
- 刮态:`LUCKY` + 「每日可刮取一次」(@704,656-1132,692)+ 刮层 1-3(@780-1138,522-632)。
- **易变值**:刮开内容 / 每日状态(**动态 / 时机性**)。

## 可交互元素

- 报刊亭场景:**刮刮卡入口**(点 → 刮态)。
- 刮态:**刮层 1-3**(drag 刮开)。
- 确认弹窗:`按钮-同类型确认`。
- 嗷呜对话分支(⚠️ 待补,screen_info 有 area 未实拍):`叫醒他` / `嗷呜被你叫醒了` / `嗷呜对话` / `嗷呜标题` / `叫醒嗷呜` / `对话选项`(代码 `scratch_card_app.py:74-94` 用)。

## 识别快照

### 1. 报刊亭场景(`news_stand`,2026-07-12 核对)

- 匹配画面:**模糊**(菜单 / 3D地图 / 画面-通用 `is_precise=False`,仅返回 area)。
- 匹配 area(本 screen 全 `id_mark=false`):

  | area | 文本 | 位置 |
  |---|---|---|
  | 刮刮卡 | 刮刮卡 | 94,956-172,1050 |

- OCR:城市快报 / CITY NEWS / 刮刮卡 / UID。

### 2. 刮态(`scratch_card_scratch`,2026-07-12 核对)

- 匹配画面:**模糊**(菜单等,仅返回 area)。
- 匹配 area(本 screen):

  | area | 文本 | 位置 |
  |---|---|---|
  | 每日可刮取一次 | 每日可刮取一次 | 704,656-1132,692 |
  | 刮层-1/2/3 | — | 780~788,522-632 |

- OCR:`LUCKY` / 每日可刮取一次 / UID。
- 布局:`LUCKY` 标题 + 刮层区域(@780-1138,522-632)+ 每日可刮取一次提示。

### 3. 确认弹窗 ⚠️ 待补

drag 刮完 → 按钮-同类型确认(@882,602-1082,654)。本次未捕捉(drag 后 OCR 无变化,今日可能已刮)。

## 备注

- **screen_info**:`news_stand.yml`(刮刮卡 / 每日可刮取一次 / 刮层 1-3 / 叫醒他 / 嗷呜* / 对话选项 / 按钮同类型确认)。
- **操作要点(手动分解,验证有效)**:
  - `transport` 报刊亭(run_operation Transport)+ **朝向重置**(先传送别的地图再回,防朝向继承)。
  - `move_w`(press_time=1)→ **sleep 1**(等到位,防交互无效 issue #2405)→ `interact`(F,**press_time=0.2** 长按)→ 报刊亭场景。
  - 点刮刮卡 → 刮态。
  - drag 刮层 1-3(duration=1.5,同 app `scratch` 节点)。
  - **每步操作后等画面动画再 capture**(操作接太快会截过渡帧)。
- **⚠️ 嗷呜对话(叫醒)/ 确认弹窗待补**(时机 / drag 结果)。
