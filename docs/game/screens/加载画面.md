---
screen_name: 加载画面
appears_in: [登录游戏]
last_updated: 2026-07-06
source_image: screens/加载画面/港口工厂旧址.webp
---

# 加载画面(lore 加载过渡)

## 何时出现
登录成功后 → **本画面** → 大世界/主菜单。游戏内的**通用加载画面**,每次加载时出现。左侧 lore 文字(世界观 tip)**会轮换**(每次不同),右侧是静态场景插画。

## 识别特征(稳定锚点)
- **左侧 lore 区域**:标题(如 `【港口工厂旧址】`)+ 描述文字 —— **内容动态轮换**,勿当特征。
- **右侧场景插画**:静态(对应 lore 的场景图)。
- **UID**:右下角(所有游戏内画面通用)。
- **无 `NOW LOADING` 文字**(那是另一个加载态 `加载中`/`loading.yml`,见备注)。

## 可交互元素
**无** —— 纯过渡加载画面,自动推进。

## 识别快照(2026-07-06 核对,_1783247956712)
- 匹配画面:**无**(`screens=[]`,未被 screen_info 收录)。
- OCR(本张 lore = 港口工厂旧址):
  | 文本 | 位置 (x,y,w×h) | 备注 |
  |---|---|---|
  | `【港口工厂旧址】` | 66,232,146×27 | lore 标题(**动态轮换**) |
  | `莱姆尼安空洞深处...辉岭矿石和辉瓷产品` | 66~68,260~310 | lore 描述(**动态**) |
  | `UID` | 1791,1058 | 右下 UID 标签 |

## 备注
- **与 `加载中`(`loading.yml`)的关系**:`loading.yml` 建的是 `NOW LOADING` 文字那个加载态;本 lore 加载画面**无 NOW LOADING**(`screens=[]` 匹配不上)。`loading.yml` 的实际用途待确认,暂忽略。
- **lore 轮换**:左侧文字是随机世界观 tip(类似原神/星铁加载界面),**不钉死具体内容**。
- **故意不建 screen_info**:本画面**无可固定的匹配特征**(lore 文字轮换 + 场景插画随 tip 变化 + 无固定按钮/文字锚点)→ 不适合做 screen_info 模板/文本 area。`screens=[]` 是**有意的缺口**,非遗漏。
