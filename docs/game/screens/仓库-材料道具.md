---
screen_name: 仓库-材料道具
appears_in: [仓库]
last_updated: 2026-07-22
source_image: 待补(未 live 实证)
---

# 仓库-材料道具

仓库的「材料道具」页(背包/材料分类),管理可合成类材料道具。是「道具处理」的另一个入口(材料道具页有「道具处理」按钮)。

> ⚠️ **未 live 实证**(2026-07-22):本画面**不在 PR #2300 的 `ExchangeEtherBattery` 流程中**(合成电池从快捷手册资源栏直接进道具处理,不经仓库),且本次未导航到仓库。下述内容基于 `storage_material.yml` 的 area 定义 + goto 连通关系整理,area 位置/模板**未经 live 截图验证**。后续导航到仓库时补 source_image 与识别快照。

## 何时出现 + 状态流转

- **入口**:仓库 → 材料道具页(具体入口待补,从「菜单」进仓库)。
- **流转**(基于 goto_list):

| 元素 | 动作 | 去向 |
|---|---|---|
| 按钮-返回 | 点 | 菜单 |
| 左上角-街区 | 点 | 大世界-普通 |
| 按钮-道具处理 | 点 | 道具处理(见 [道具处理](道具处理.md)) |
| TAB-音擎 | 点 | 仓库-音擎仓库 |
| TAB-驱动盘 | 点 | 仓库-驱动仓库 |

## 识别特征(稳定锚点,待 live 校验)

- **标题「材料道具」**:左上(pc_rect ≈ 70,116 - 170,164),`id_mark`。

## 可交互元素(基于 screen_info,待 live 校验)

| 元素 | 类型 | 备注 |
|---|---|---|
| 标题-材料道具 | text(id_mark) | 画面识别锚点 |
| 按钮-返回 | template(menu/back) | goto 菜单 |
| 左上角-街区 | text | goto 大世界-普通 |
| 按钮-道具处理 | text | goto 道具处理 |
| TAB-音擎 | template | goto 仓库-音擎仓库(图标 tab) |
| TAB-驱动盘 | template | goto 仓库-驱动仓库(图标 tab) |

## 备注

- **代码零引用**:本画面 `screen_id=storage_material` 在 `src/` 无任何引用,纯画面连通图节点。PR #2300 定义它应是为画面连通完整性 + 仓库入口的备选路径。
- **`ExchangeEtherBattery` 实际路径**:快捷手册资源栏点以太电池 → 「[获取]合成」→ 道具处理,**不经过**本画面。
