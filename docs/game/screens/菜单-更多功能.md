---
screen_name: 菜单-更多功能
appears_in: [菜单]
last_updated: 2026-07-11
source_image: screens/菜单-更多功能/默认.webp
---

# 菜单-更多功能

## 何时出现 + 状态流转

菜单底部点「更多」tab → **菜单-更多功能**(模态)。功能入口枢纽,多个 app 经此进入(预备编队 / 兑换码 / 资源管理 / 登出…)。

| 出口 | 动作→下一态 |
|---|---|
| 功能按钮 | 点对应功能 → 各功能画面(兑换码→兑换码输入;登出→登出确认;预备编队→…) |
| 关闭X / 返回 | → 菜单 |

## 识别特征(稳定锚点)

- **标题「更多功能」**(@~(536,224),`标题-更多功能` area,`id_mark`)。
- **按钮-关闭**(模板 `menu/btn_close` @~(1302,166-1444,282),`id_mark`;红色圆形 X)。
- 功能网格(3 行 5 列,黑色方形按钮 + 白图标/文字)。
- **易变值**:NEW! 徽章(有新内容按钮上出现,查看后消失);功能项可能随版本增减。

## 可交互元素

- 功能按钮(vision 核对 12 项):训练场 / 预备编队 / 绳匠笔记 / 养成指南 / 限定活动 / 社群 / 兑换码 / 反馈 / 云游戏 / 脱离卡死 / 资源管理 / 登出(部分格子 EMPTY)。
- 关闭X(右上红圆)。
- 模态:半透明遮罩透出主菜单(vision 确认)。

## 识别快照(`menu_more_default`,2026-07-11 核对)

- 匹配画面:**菜单-更多功能** (`is_precise=True` ✓)
- 匹配 area:

  | area | 类型 | 文本 | conf | 位置 (x,y,w×h) |
  |---|---|---|---|---|
  | 标题-更多功能 | text | 更多功能 | 0.9998 | 536,224,119×36 |
  | 按钮-关闭 | template(`menu/btn_close`) | — | 0.9935 | 1342,202,52×50 |
  | 按钮-预备编队 | text | 预备编队 | 0.9997 | 750,463,73×25 |
  | 按钮-兑换码 | text | 兑换码 | 0.9854 | 758,645,59×25 |

- OCR(核心):更多功能 / 训练场 / 预备编队 / 绳匠笔记 / 养成指南 / 限定活动 / 社群 / 兑换码 / 反馈 / 云游戏 / 脱离卡死 / 资源管理 / 登出。

## 备注

- **已建模** `menu_more.yml`(4 area:标题 / 关闭 / 预备编队 / 兑换码),is_precise。模态弹窗。
- screen_info 只示例性建了「预备编队」「兑换码」两个功能按钮 area;其余(训练场 / 资源管理 / 登出等)**未建 area**,按需补(对应 app 建档时)。
- 入口导航:大世界(`pc_alt=true`)→ 点左上菜单按钮 → 菜单 → 底部「更多」tab → 菜单-更多功能。
