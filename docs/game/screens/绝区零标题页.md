---
screen_name: 绝区零标题页
appears_in: [游戏启动]
last_updated: 2026-07-05
source_image: screens/绝区零标题页/默认.webp
---

# 绝区零标题页(游戏 logo)

## 何时出现
打开游戏后,**「警告:游戏前详阅」之后、登录「打开游戏」之前**的游戏标题 / logo 展示页。展示数秒(或点击)后进入登录。

## 识别特征(稳定锚点,供 screen_info)
- **中文标题** `绝·区·零`:@(827,691,268×76),画面下方居中(艺术字)。
- 下方英文游戏名 `ZENLESS ZONE ZERO`(艺术字,OCR 误识成 `ZRNLESS ZONE ZRrO`,不可靠)。

> 用**中文标题** `绝·区·零` 或 logo 图像(模板匹配)做特征;英文艺术字 OCR 不稳,别依赖。

## 可交互元素
**无明显按钮**(OCR 未识别到)—— 推测自动推进或点击继续,待确认。

## 识别快照(2026-07-05 核对)

**匹配画面**:无(`screens=[]`,未被任何 screen_info 收录)

**匹配 area**:无

**OCR 文本**(全量):
| 文本 | 位置 (x,y,w×h) | 备注 |
|---|---|---|
| `绝·区·零` | 827,691,268×76 | 稳定(中文标题,识别锚点) |
| `ZRNLESS ZONE ZRrO` | 835,768,249×20 | 英文 `ZENLESS ZONE ZERO` 的 OCR 误识(艺术字,不稳定) |

## 备注
- **screen_info 缺口**:`screens:[]`(连模糊匹配都没有)。补 screen_info 用中文标题 `绝·区·零` 或 logo 模板图做特征。
- **OCR 局限**:艺术字英文 OCR 不可靠,识别方案优先中文标题或图像模板。
- **变更检测**:重新截一张本画面跑 `analyze_screen` 与上方快照 diff;logo 极少变,变了就更新快照 + `last_updated`/`source_image`。
